const text = `Este es un texto con "comillas dobles" y 'comillas simples' dentro de un string`;

const stringMultiline = `Este es un string
multilinea
con saltos de linea`;

// Interpolacion de variables con template string
const name = "Jeremy";
const greeting = `Hola ${name}, bienvenido a TypeScript`;

console.log(text);
console.log(stringMultiline);
console.log(greeting);

console.log("");
