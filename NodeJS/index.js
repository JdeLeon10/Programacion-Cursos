import { sum } from './math.js'; // Para importar en node JS debemos utilizar la extension .js
const resultado = sum(2, 3);
console.log(`El resultado de la suma es: ${resultado}`);

console.log("Hello, World!");