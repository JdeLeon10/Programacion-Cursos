import 'package:flutter/material.dart';

// Clase centralizada para los colores de la aplicacion.
// Esto evita tener colores repetidos en muchas pantallas.
class AppColors {
  const AppColors._();

  static const Color primary = Color(0xFF355C7D);
  static const Color background = Color(0xFFF3F6FA);
  static const Color drawerHeader = Color(0xFF2A445E);
  static const Color card = Colors.white;
}
